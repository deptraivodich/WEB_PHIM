rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser SDK access is disabled. Backend Admin SDK enforces its own permissions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
